const path = require('path')
const { Migrator } = require('../dist/migrator')

test('it works up', async () => {
  // const migrationsDir = path.join(__dirname, 'migrations')
  // const migrator = new Migrator(migrationsDir)
  // migrator.up()
})

test('it works down', async () => {
  // const migrationsDir = path.join(__dirname, 'migrations')
  // const migrator = new Migrator(migrationsDir)
  // migrator.down()
})
